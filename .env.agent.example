# No Johns Agent — Environment Variables
# Copy to .env.agent and fill in your values.
# See docs/SETUP-DOCKER.md for details.

# REQUIRED: Path to your Melee ISO on the host machine
ISO_PATH=/path/to/melee.iso

# REQUIRED: Your Slippi connect code (create via Slippi Launcher GUI)
CONNECT_CODE=ABCD#123

# Arena server URL (default: public arena)
ARENA_URL=https://nojohns-arena-production.up.railway.app

# Netplay tuning (defaults are fine for most setups)
ONLINE_DELAY=6
INPUT_THROTTLE=1

# OPTIONAL: Wallet for onchain records (Tier 2+)
# Generate with: nojohns setup wallet
# PRIVATE_KEY=0x...
# CHAIN_ID=143
# RPC_URL=https://rpc.monad.xyz
